name: family_board
description: Family bulletin board app — leave memos on the TV for family.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  lisa_appmcp_flutter_sdk:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
